/**
 * Shared asset package for files consumed by multiple apps in the monorepo.
 *
 * Import image files directly from subpaths, for example:
 * `import heroImage from "@workspace/assets/images/hero.png"`
 */
export {}
